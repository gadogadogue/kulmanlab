---
title: "এক্সপোর্ট ম্যানেজার — ড্রয়িং DXF বা JSON হিসেবে ডাউনলোড করুন"
description: "এক্সপোর্ট ম্যানেজার বর্তমান ড্রয়িংকে DXF বা JSON (নেটিভ) ফাইল হিসেবে ডাউনলোড করে। প্রতিটি ফরম্যাট পাশাপাশি ঠিক কোন এনটিটি টাইপ বহন করে তা তালিকাভুক্ত করে, যাতে ডাউনলোডের আগেই দেখতে পান DXF কী বাদ দেয় — বর্তমানে Hatches, Dimensions, Leaders, এবং Text।"
keywords: [CAD DXF এক্সপোর্ট, CAD ফাইল এক্সপোর্ট, ব্রাউজারে DXF ডাউনলোড, DXF অনলাইনে সেভ, JSON CAD এক্সপোর্ট, KulmanLab এক্সপোর্ট, CAD ফাইল ডাউনলোড, DXF এক্সপোর্ট, ড্রয়িং ফাইলে সেভ, DXF ডাউনলোড]
group: file
order: 5
---

# এক্সপোর্ট ম্যানেজার

`exportmanager` কমান্ড বর্তমান ড্রয়িংকে আপনার ফাইল সিস্টেমে ডাউনলোড করে। পাশাপাশি কার্ড হিসেবে দুটি ফরম্যাট পাওয়া যায়: অন্যান্য CAD টুলের সাথে সামঞ্জস্যের জন্য **DXF** এবং KulmanLab CAD-এর মধ্যে সম্পূর্ণ নির্ভুলতার সাথে সেভের জন্য **JSON** — প্রতিটি কার্ড ঠিক কোন এনটিটি টাইপ সেই ফরম্যাট বহন করে তা তালিকাভুক্ত করে।

## এক্সপোর্ট করার উপায়

1. ফাইল প্যানেলে টুলবারের **Export** বাটনে (ডাউনলোড আইকন) ক্লিক করুন, অথবা টার্মিনালে `exportmanager` টাইপ করুন।
2. **এক্সপোর্ট ম্যানেজার** পপআপ খোলে, যা JSON এবং DXF কার্ড পাশাপাশি দেখায়, প্রতিটি কী এক্সপোর্ট হয় (এবং DXF-এর ক্ষেত্রে, কী বাদ যায়) তা তালিকাভুক্ত করে।
3. ফরম্যাট নির্বাচন করতে একটি কার্ডে ক্লিক করুন — **JSON** বা **DXF**।
4. **Export \<FORMAT\>** বাটনে ক্লিক করুন। ফাইলটি স্বয়ংক্রিয়ভাবে আপনার ডিফল্ট ডাউনলোড ফোল্ডারে ডাউনলোড হয়।

এক্সপোর্ট না করে পপআপ বন্ধ করতে `Escape` চাপুন।

## একটি ফরম্যাট বাছাই করা

| ফরম্যাট | এক্সটেনশন | সেরা যেখানে | সীমাবদ্ধতা |
|--------|-----------|------------|-------------|
| **JSON** *(নেটিভ)* | `.json` | KulmanLab CAD-এ আবার খোলার জন্য কাজ সেভ করা | অন্যান্য CAD টুলের সাথে সামঞ্জস্যপূর্ণ নয় |
| **DXF** | `.dxf` | FreeCAD, LibreCAD ইত্যাদির সাথে শেয়ার করা | Hatches, Dimensions, Leaders, এবং Text এক্সপোর্ট হয় না |

**কখন JSON ব্যবহার করবেন:** যখনই আপনি আপনার কাজের একটি সম্পূর্ণ কপি সেভ করতে চান। JSON হল KulmanLab-এর নেটিভ ফরম্যাট এবং প্রতিটি এনটিটি নিখুঁতভাবে সংরক্ষণ করে — Dimensions, Leaders, Hatches, এবং সমস্ত লেয়ার ডেটাসহ।

**কখন DXF ব্যবহার করবেন:** যখন আপনাকে অন্য CAD অ্যাপ্লিকেশন ব্যবহারকারী কাউকে ড্রয়িং হস্তান্তর করতে হয়। এক্সপোর্ট করা ফাইলটি AC1032 DXF ফরম্যাট ব্যবহার করে এবং বেশিরভাগ DXF-সামঞ্জস্যপূর্ণ টুলে খোলা যায়।

## প্রতিটি ফরম্যাটে কী এক্সপোর্ট হয়

### JSON এক্সপোর্ট

প্রতিটি এনটিটি টাইপ অন্তর্ভুক্ত থাকে:

- Lines, Circles, Arcs, Ellipses, Polylines, Splines
- Text
- Dimensions (linear, aligned, continued, radius, diameter)
- Leaders (multileaders)
- Hatches, তাদের প্যাটার্ন, স্কেল, কোণ এবং origin সহ
- Layers এবং Linetypes

### DXF এক্সপোর্ট

শুধুমাত্র জ্যামিতিক এনটিটি অন্তর্ভুক্ত থাকে:

- Lines, Circles, Arcs, Ellipses, Polylines (`LWPOLYLINE` হিসেবে এক্সপোর্ট), Splines
- Layers এবং Linetypes

**DXF-এ এক্সপোর্ট হয় না:** Hatches, Dimensions, Leaders, এবং Text। Dimensions এবং Leaders KulmanLab-নির্দিষ্ট ডেটা স্ট্রাকচার ব্যবহার করে যা স্ট্যান্ডার্ড DXF-এ বিশ্বস্তভাবে উপস্থাপন করা যায় না; Hatches এখনো DXF-এ একেবারেই এক্সপোর্ট হয় না, যদিও সেগুলো DXF থেকে ইমপোর্ট হয়; Text এক্সপোর্টও এখনো বাস্তবায়িত হয়নি। আপনার ড্রয়িংয়ে এর কোনোটি থাকলে, সেগুলো ধরে রাখতে JSON বা [প্রিন্ট ম্যানেজার](../print-manager/) ব্যবহার করুন।

## এক্সপোর্ট করা ফাইলের নাম

ডাউনলোড করা ফাইলের নাম বর্তমান ড্রয়িং ফাইলের নামে হয় (যেমন `myplan.json`)। নির্বাচিত ফরম্যাট অনুযায়ী এক্সটেনশন পরিবর্তিত হয়।

## এক্সপোর্ট ম্যানেজার এবং প্রিন্ট ম্যানেজারের মধ্যে পার্থক্য

| বৈশিষ্ট্য | এক্সপোর্ট ম্যানেজার | প্রিন্ট ম্যানেজার |
|-----------|---------------------|-------------------|
| আউটপুট | ভেক্টর সোর্স ফাইল (.dxf / .json) | র‍্যাস্টার ছবি (.png / .jpeg / .webp / .pdf) |
| অন্য টুলে সম্পাদনাযোগ্য | হ্যাঁ (DXF) | না |
| Layers ও Linetypes সংরক্ষণ করে | হ্যাঁ | না (ফ্ল্যাট রেন্ডার হয়) |
| Dimensions ও Leaders ধরে রাখে | শুধু JSON | হ্যাঁ |

সম্পাদনাযোগ্য ফাইল প্রয়োজন হলে **এক্সপোর্ট ম্যানেজার** ব্যবহার করুন। ভিজ্যুয়াল স্ন্যাপশট প্রয়োজন হলে [প্রিন্ট ম্যানেজার](../print-manager/) ব্যবহার করুন।

## সম্পর্কিত কমান্ড

- [Import](../import/) — একটি DXF বা JSON ফাইল খুলুন
- [প্রিন্ট ম্যানেজার](../print-manager/) — ক্যানভাসকে PNG, JPEG, WebP, বা PDF ছবি হিসেবে এক্সপোর্ট করুন
- [File Manager](../file-manager/) — ব্রাউজার স্টোরেজে সংরক্ষিত ড্রয়িং ব্রাউজ করুন
