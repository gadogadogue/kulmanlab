---
title: FontAdd — Завантажити власний TTF-шрифт з терміналу
description: Команда FontAdd відкриває системний вибір файлу для завантаження шрифту .ttf, не відкриваючи спочатку діалог Font Manager. Це те саме завантаження, яке запускає кнопка «Add Font» у Font Manager, доступне тут як окрема команда терміналу.
keywords: [команда font add, команда fontadd, завантажити ttf термінал, власний шрифт CAD, kulmanlab]
group: style
order: 3
---

# FontAdd

Команда `FontAdd` відкриває системний вибір файлу для завантаження власного шрифту `.ttf`, не відкриваючи спочатку діалог [Font Manager](../font-manager/). Це те саме завантаження, яке запускає кнопка **Add Font** у Font Manager — FontAdd це просто прямий шлях до нього з терміналу.

## Завантаження шрифту

1. Введіть `FontAdd` у терміналі або натисніть **Add Font** у нижній частині діалогу [Font Manager](../font-manager/).
2. Виберіть файл `.ttf` у системному вікні вибору. Підтримуються лише шрифти TrueType — `.otf` та `.woff`/`.woff2` не підтримуються.

Команда завершується одразу після відкриття вікна вибору файлу — жодного подальшого кліку чи введення в терміналі не потрібно. Шрифт реєструється й з'являється в групі **User** одразу після вибору файлу.

## Що відбувається при завантаженні

- Назва файлу (без розширення) стає назвою шрифту. Завантаження `MyFont.ttf` додає шрифт з назвою `MyFont`.
- Завантаження файлу, назва якого збігається з наявним власним шрифтом, **замінює** його.
- Шрифт зберігається назавжди в браузері (IndexedDB) і автоматично завантажується знову при наступному відкритті KulmanLab CAD — він не прив'язаний до поточного креслення.

## Довідник клавіатури

FontAdd не має власної взаємодії з клавіатурою — уся команда зводиться до нативного вікна вибору файлу браузера. Скасування цього діалогу (або невибір файлу) залишає список шрифтів без змін.

## Пов'язані команди

| Команда | Що робить |
|---------|-----------|
| [Font Manager](../font-manager/) | Переглядайте, попередньо переглядайте, вибирайте та видаляйте шрифти, включно з власними завантаженнями |
| [Text](../text/) | Розміщує текстові мітки, до яких застосовується вибір шрифту |
