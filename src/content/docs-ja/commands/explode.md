---
title: Explode コマンド — ポリラインをラインとアークのエンティティに分解
description: Explode コマンドはポリラインをその場で個々のラインとアークのエンティティに、セグメントごとに1つずつ分解します。各パーツは元のポリラインの線幅、色、レイヤー、線種を引き継ぎます。Polyline エンティティのみに対応しています。
keywords: [CAD explode コマンド, ポリライン分解 CAD, ポリラインをラインに分解, ポリラインをラインとアークに変換, kulmanlab]
group: edit
order: 16
---

# Explode

`explode` コマンドは[ポリライン](../polyline/)を個々の[ライン](../line/)と[アーク](../arc/)のエンティティに分解します — セグメントごとに1つずつ、ちょうどポリライン自身の頂点があった位置に。分解されたパーツはポリラインをその場で置き換え、その線幅、色、レイヤー、線種を引き継ぎます。

Explode は **Polyline** エンティティにのみ対応しています。

## explode の使い方

実行方法は2通りあり、[Delete](../delete/)と同じパターンです：

**先に選択してから explode** — 最も速い方法：

1. キャンバス上で1つ以上のポリラインを選択します。
2. ターミナルに `explode` と入力するか、Edit パネルの **Explode** ボタンをクリックします。

選択したポリラインは即座に分解されます — すでに何かが選択されているため、別途確認のステップはありません。

**コマンドを起動してから選択**：

1. 何も選択していない状態で `explode` と入力するか、ツールバーのボタンをクリックします。
2. **ポリラインを選択** — クリックでトグル、またはドラッグで範囲選択します。
3. **Enter** または **スペース** を押して、選択したポリラインを確定して分解します。

選択中に対象となるのはポリラインのみです — Line、Circle、その他のエンティティをクリックしても何も起こらず、範囲ドラッグもボックス内または境界と交差するポリライン以外はすべて無視します。

## 分解すると何が得られるか

ポリラインの各セグメントはそれぞれ独立したエンティティになります：

- **直線セグメント**は**ライン**になります。
- **円弧セグメント**（Polyline の[Arc オプション](../polyline/)によるもの）は、元の曲線の中心・半径・スイープに正確に一致する**アーク**になります。

生成された各ラインとアークは、元のポリラインの**線幅、色、レイヤー、線種、線種尺度**を引き継ぎます — ジオメトリの見た目は何も変わらず、1つの連結されたポリラインだった代わりに複数の独立したエンティティになっただけです。

分解は他の編集操作と同じく、[Undo](../undo/) で1ステップとして取り消せます。

## コマンド実行中の選択

| 方法 | 動作 |
|------|------|
| **クリック** | カーソル下のポリラインを選択のオン/オフで切り替え；ポリライン以外のエンティティをクリックしても何も起こらない |
| **右方向ドラッグ**（厳密） | ボックスに完全に収まるポリラインのみを選択 |
| **左方向ドラッグ**（交差） | ボックスの境界と交差するポリラインを選択 |
| **Enter** / **スペース** | 選択したポリラインを確定して分解 |

## サポートされるエンティティ

| エンティティ | サポート |
|--------|---------|
| Polyline / Rectangle | あり |
| Line, Arc, Circle, Ellipse | なし — 分解するものがない |
| Text, Spline, Dimension, Leader, Hatch | なし |
