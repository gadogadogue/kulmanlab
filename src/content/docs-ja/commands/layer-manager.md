---
title: LayerManager コマンド — すべてのレイヤーを1つの表で管理（KulmanLab CAD）
description: LayerManager コマンドは図面のすべてのレイヤーの表を開き、レイヤーの追加と、各レイヤーのフリーズ・ロック・プロット・色・線幅・線種をその場で編集できます。
keywords: [レイヤーマネージャー, CAD レイヤー表, レイヤー管理 CAD, レイヤー追加 CAD, フリーズ ロック プロット レイヤー, kulmanlab レイヤー管理]
group: layer
order: 1
---

# LayerManager

`LayerManager` コマンドは、図面の全レイヤーを一覧表示する表を開きます。**Freeze**（フリーズ）、**Lock**（ロック）、**Plot**（プロット）、**色**、**線幅**、**線種** を各行で直接編集できます。新しいレイヤーを追加し、既存レイヤーの挙動を調整する中心的な場所です — 他のレイヤーコマンド（[LayerMakeCurrent](../layer-make-current/)、[LayerMatch](../layer-match/)、[LayerIsolate](../layer-isolate/)、[LayerUnfreezeAll](../layer-unfreeze-all/)）は、これを開かずにそれぞれ一つの絞られた操作を行います。

## Layer Manager を開く

- ターミナルに `LayerManager` と入力する、**または**
- レイヤーパネルの **Layer Manager** ボタンをクリックします。

ダイアログはフローティングパネルとして開きます。事前に何かを選択しておく必要はありません。

## レイヤー表

| 列 | 制御する内容 |
|----|----------------|
| Name | レイヤー名。表内では読み取り専用で表示されます(作成時に一度だけ設定) |
| Freeze | レイヤーのエンティティを非表示にし、解除するまで選択対象から除外します |
| Lock | レイヤー上のエンティティの編集を、非表示にせずに禁止します |
| Plot | レイヤーのエンティティを印刷や PDF エクスポートに含めるかどうか |
| Color | レイヤーの ACI 色 — スウォッチをクリックしてカラーピッカーを開きます |
| Lineweight | レイヤーの線幅 — チップをクリックして線幅ピッカーを開きます |
| Linetype | レイヤーの破線パターン — チップをクリックして線種ピッカーを開きます |

Freeze、Lock、Plot の切り替えは即座に反映されます — 別途保存の手順はありません。色・線幅・線種が **ByLayer**(既定値)に設定されているエンティティは、ここで設定した内容を引き継ぎます。独自の明示的な上書きを持つエンティティは影響を受けません。

## レイヤーを追加する

1. 表の下部にある **+ Add Layer** をクリックします。
2. 名前を入力して **Enter** で確定するか、**Escape** でキャンセルします。

レイヤー名には英数字、スペース、`_`、`-`、`$` を使用できます。空の名前、既に使用されている名前、それ以外の文字を含む名前は、インラインのエラーとともに拒否され、行は再入力のために開いたままになります。

新しいレイヤーは**フリーズ解除、ロック解除、プロット可能**な状態で始まり、色は 7(白/黒)、線幅は Default、線種は Continuous です — これは [Import](../import/) が空の図面のレイヤー `0` に割り当てるのと同じ既定値です。

## ここでできないこと

削除ボタンはありません — レイヤーは作成後に削除されることはなく、フリーズするか未使用のまま残すことしかできません。表にはどのレイヤーが*カレント*かも表示されません。それはレイヤーパネルのドロップダウンや [LayerMakeCurrent](../layer-make-current/) から設定するもので、このダイアログからではありません。

## キーボードリファレンス

| キー | 操作 |
|-----|------|
| `Enter` | 新しいレイヤー名を確定する(追加中) |
| `Escape` | レイヤーの追加をキャンセルする、またはダイアログを閉じる |

## 関連コマンド

| コマンド | 機能 |
|---------|------|
| [LayerMakeCurrent](../layer-make-current/) | クリックしたエンティティのレイヤーをカレントレイヤーに設定する |
| [LayerMatch](../layer-match/) | 選択エンティティをソースエンティティのレイヤーに合わせて変更する |
| [LayerIsolate](../layer-isolate/) | 選択エンティティのレイヤー以外をすべてフリーズする |
| [LayerUnfreezeAll](../layer-unfreeze-all/) | すべてのレイヤーを一括でフリーズ解除する |
