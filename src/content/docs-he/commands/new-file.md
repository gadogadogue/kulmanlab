---
title: New File — התחלת שרטוט ריק ב-KulmanLab CAD
description: פקודת New File מנקה את הקנבס ופותחת שרטוט ריק חדש. שם קובץ פשוט נוצר אוטומטית ונשמר באחסון הדפדפן.
keywords: [קובץ CAD חדש, שרטוט חדש, קנבס ריק CAD, יצירת שרטוט חדש מקוון, התחלת DXF חדש, קובץ חדש KulmanLab, איפוס קנבס, ניקוי שרטוט]
group: file
order: 2
---

# New File

הפקודה **New File** מנקה את הקנבס ומתחילה שרטוט ריק חדש. שם קובץ ייחודי נוצר אוטומטית.

## איך ליצור קובץ חדש

לחצו על כפתור סרגל הכלים **New File** (סמל עמוד חדש) בפאנל File. הקנבס מתנקה מיידית — ללא הנחיות או דיאלוגי אישור.

## מה הקובץ החדש מכיל

קובץ שנוצר לאחרונה מתחיל עם:

- **ללא ישויות** על הקנבס.
- **שכבת ברירת מחדל אחת** בשם `0` עם צבע לבן וסוג קו `Continuous`.
- **שם קובץ שנוצר**, `kulman.dxf` — או `kulman (2).dxf`, `kulman (3).dxf`, … אם השם כבר תפוס.

הקובץ נשמר באחסון הדפדפן אוטומטית ומופיע ב-[File Manager](../file-manager/), וניתן [לשנות את שמו](../file-manager/#שינוי-שם-קובץ) בכל עת.

## אזהרה — עבודה לא שמורה נמחקת

לחיצה על **New File** מוחקת את כל הישויות בקנבס הנוכחי ללא אזהרה. אם ברצונכם לשמור את השרטוט הנוכחי, [ייצאו](../export/) אותו קודם.

## מתי להשתמש ב-New File מול Import

| מצב | פעולה מומלצת |
|-----------|-------------------|
| התחלת שרטוט מאפס | **New File** |
| פתיחת קובץ DXF או JSON קיים | [Import](../import/) |
| העתקת שרטוט כדי לעבוד על גרסה | [ייצאו](../export/) את הקובץ הנוכחי, ואז [ייבאו](../import/) את ההעתק |

## פקודות קשורות

- [Import](../import/) — פתיחת שרטוט DXF או JSON קיים
- [Export](../export/) — הורדת השרטוט לפני התחלה מחדש
- [File Manager](../file-manager/) — שחזור שרטוט קודם מאחסון הדפדפן
