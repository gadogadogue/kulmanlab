---
title: "مدير التصدير — تصدير الرسومات بصيغة DXF أو JSON"
description: "يقوم مدير التصدير بتنزيل الرسم الحالي كملف DXF أو JSON (أصلي). يسرد كل تنسيق بالضبط أنواع الكيانات التي يحملها، جنباً إلى جنب، حتى ترى قبل التنزيل ما الذي يتركه DXF — حالياً: Hatches وDimensions وLeaders وText."
keywords: [تصدير DXF, تصدير ملف CAD, تنزيل DXF من المتصفح, حفظ DXF اونلاين, تصدير JSON CAD, تصدير KulmanLab, تنزيل ملف CAD, تصدير DXF, حفظ رسم كملف, تنزيل DXF]
group: file
order: 5
---

# مدير التصدير

يقوم أمر `exportmanager` بتنزيل الرسم الحالي إلى نظام الملفات لديك. يتوفر تنسيقان، معروضان كبطاقات جنباً إلى جنب: **DXF** للتوافق مع أدوات CAD الأخرى، و**JSON** لحفظ كامل الدقة داخل KulmanLab CAD — تسرد كل بطاقة بالضبط أنواع الكيانات التي يحملها ذلك التنسيق.

## كيفية التصدير

1. انقر على زر **Export** في شريط الأدوات (أيقونة التنزيل) في لوحة الملف، أو اكتب `exportmanager` في المحطة.
2. تفتح نافذة **مدير التصدير** المنبثقة معروضة بطاقتي JSON وDXF جنباً إلى جنب، كل منهما تسرد ما يتم تصديره (وبالنسبة لـ DXF، ما يتم تركه).
3. انقر على بطاقة لاختيار التنسيق — **JSON** أو **DXF**.
4. انقر على زر **Export \<FORMAT\>**. يُنزَّل الملف تلقائياً إلى مجلد التنزيلات الافتراضي لديك.

اضغط `Escape` لإغلاق النافذة المنبثقة دون تصدير.

## اختيار التنسيق

| التنسيق | الامتداد | الأنسب لـ | القيود |
|---------|---------|----------|--------|
| **JSON** *(أصلي)* | `.json` | حفظ العمل لإعادة فتحه في KulmanLab CAD | غير متوافق مع أدوات CAD الأخرى |
| **DXF** | `.dxf` | المشاركة مع FreeCAD وLibreCAD وغيرها | لا يتم تصدير Hatches وDimensions وLeaders وText |

**متى تستخدم JSON:** في أي وقت تريد فيه حفظ نسخة كاملة من عملك. JSON هو تنسيق KulmanLab الأصلي ويحافظ على كل عنصر بدقة تامة — بما في ذلك Dimensions وLeaders وHatches وجميع بيانات الطبقات.

**متى تستخدم DXF:** عندما تحتاج إلى تسليم الرسم لشخص يستخدم تطبيق CAD آخر. يستخدم الملف المُصدَّر صيغة DXF من نوع AC1032 ويمكن فتحه في معظم الأدوات المتوافقة مع DXF.

## ما الذي يُصدَّر لكل تنسيق

### تصدير JSON

يتم تضمين كل نوع من الكيانات:

- Lines وCircles وArcs وEllipses وPolylines وSplines
- Text
- Dimensions (خطي، محاذٍ، متسلسل، نصف قطر، قطر)
- Leaders (multileaders)
- Hatches، بما في ذلك النمط والمقياس والزاوية ونقطة الأصل
- Layers وLinetypes

### تصدير DXF

يتم تضمين عناصر الهندسة فقط:

- Lines وCircles وArcs وEllipses وPolylines (تُصدَّر كـ `LWPOLYLINE`) وSplines
- Layers وLinetypes

**غير مُصدَّر إلى DXF:** Hatches وDimensions وLeaders وText. تستخدم Dimensions وLeaders بنى بيانات خاصة بـ KulmanLab لا يمكن تمثيلها بأمانة في DXF القياسي؛ لا يتم تصدير Hatches إلى DXF على الإطلاق حتى الآن، رغم أنها تُستورد منه؛ تصدير Text أيضاً لم يُنفَّذ بعد. إذا كان رسمك يحتوي على أي من هذه، استخدم JSON أو [مدير الطباعة](../print-manager/) لالتقاطها.

## اسم الملف المُصدَّر

يُسمَّى الملف المُنزَّل بحسب ملف الرسم الحالي (مثلاً `myplan.json`). يتغير الامتداد ليطابق التنسيق المختار.

## الفرق بين مدير التصدير ومدير الطباعة

| الميزة | مدير التصدير | مدير الطباعة |
|--------|--------------|--------------|
| المخرَج | ملف مصدر متجهي (.dxf / .json) | صورة نقطية (.png / .jpeg / .webp / .pdf) |
| قابل للتحرير في أدوات أخرى | نعم (DXF) | لا |
| يحافظ على Layers وLinetypes | نعم | لا (يُعرَض بشكل مسطح) |
| يلتقط Dimensions وLeaders | JSON فقط | نعم |

استخدم **مدير التصدير** عندما تحتاج إلى ملف قابل للتحرير. استخدم [مدير الطباعة](../print-manager/) عندما تحتاج إلى لقطة مرئية.

## أوامر ذات صلة

- [Import](../import/) — فتح ملف DXF أو JSON
- [مدير الطباعة](../print-manager/) — تصدير اللوحة كصورة PNG أو JPEG أو WebP أو PDF
- [File Manager](../file-manager/) — تصفح الرسومات المحفوظة في تخزين المتصفح
